#include<stdio.h>
/**
 * main - a c program to print a piece of words
 * Return: return 0 if noerrors
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
